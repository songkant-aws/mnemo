# Mnemo Query — search local memory

$ARGUMENTS

```bash
mnemo query --question "$ARGUMENTS"
```

Use the result to answer with:

- `matched_entities`
- `contexts`
- `relations`
- `expanded_entities`
